export default function SettingsPage(){return <><div className="page-title"><div><span>SETTINGS</span><h2>Data & Brain configuration</h2></div><p>Safe defaults keep secrets on the backend and let the project run without broker credentials.</p></div><section className="two-col"><div className="panel"><div className="panel-kicker">DEFAULT MARKET SOURCE</div><h2>Gold API</h2><div className="settings-list"><div><span>Authentication</span><strong>None for current spot endpoint</strong></div><div><span>Refresh</span><strong>30 seconds</strong></div><div><span>Symbol</span><strong>XAU / USD</strong></div><div><span>Storage</span><strong>Local persistent database</strong></div></div></div><div className="panel"><div className="panel-kicker">OPTIONAL BOOTSTRAP</div><h2>Twelve Data</h2><p className="muted">If you later add a key to the backend .env file, the service can bootstrap historical intraday candles. The site does not require it.</p><code className="code-line">TWELVE_DATA_API_KEY=</code></div></section><section className="panel info-panel"><div className="panel-kicker">IMPORTANT</div><h2>No frontend secrets</h2><p>Never put API keys, broker credentials, database passwords, or private tokens in NEXT_PUBLIC_* variables. Anything beginning with NEXT_PUBLIC_ is visible to website visitors.</p></section></>}
